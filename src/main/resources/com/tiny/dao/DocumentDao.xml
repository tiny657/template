<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.tiny.dao.DocumentDao">
	<resultMap id="result" type="document">
		<result property="documentId" column="documentId" />
		<result property="content" column="content" />
		<result property="point" column="point" />
		<result property="like" column="like" />
		<result property="dislike" column="dislike" />
		<result property="comment" column="comment" />
		<result property="sharing" column="sharing" />
		<result property="providerUserId" column="providerUserId" />
		<result property="name" column="name" />
		<result property="ipAddress" column="ipAddress" />
		<result property="regDate" column="regDate" />
	</resultMap>
	
	<insert id="save" parameterType="document">
		INSERT
		INTO document 
				(`content`,
				`point`,
				`like`,
				`dislike`,
				`comment`,
				`sharing`,
				`providerUserId`,
				`name`,
				`ipAddress`,
				`regDate`)
		VALUES (#{content},
				#{point},
				#{like},
				#{dislike},
				#{comment},
				#{sharing},
				#{providerUserId},
				#{name},
				#{ipAddress},
				NOW());
	</insert>
	
	<select id="count" resultType="int">
		SELECT COUNT(*)
		FROM document;
	</select>
	
	<select id="getAll" resultMap="result">
		SELECT *
		FROM document
		ORDER BY documentId DESC;
	</select>
	
	<select id="get" resultType="document">
		SELECT *
		FROM document
		WHERE documentId = #{documentId};
	</select>
	
	<select id="getProviderUserId" resultType="String">
		SELECT providerUserId
		FROM document
		WHERE documentId = #{documentId};
	</select>
	
	<select id="getLast" resultType="document">
		SELECT *
		FROM document
		WHERE documentId = LAST_INSERT_ID();
	</select>
	
	<update id="increaseComment" parameterType="document">
  		UPDATE document
  		SET
			`comment` = `comment` + 1
  		WHERE documentId = #{documentId};
	</update>
	
	<update id="decreaseComment" parameterType="document">
  		UPDATE document
  		SET
			`comment` = `comment` - 1
  		WHERE documentId = #{documentId};
	</update>
	
	<update id="increaseLike" parameterType="document">
  		UPDATE document
  		SET
			`like` = `like` + 1
  		WHERE documentId = #{documentId};
	</update>
	
	<update id="decreaseLike" parameterType="document">
  		UPDATE document
  		SET
			`like` = `like` - 1
  		WHERE documentId = #{documentId};
	</update>
	
	<update id="increaseDislike" parameterType="document">
  		UPDATE document
  		SET
			`dislike` = `dislike` + 1
  		WHERE documentId = #{documentId};
	</update>
	
	<update id="decreaseDislike" parameterType="document">
  		UPDATE document
  		SET
			`dislike` = `dislike` - 1
  		WHERE documentId = #{documentId};
	</update>
	
	<update id="increaseSharing" parameterType="document">
  		UPDATE document
  		SET
			`sharing` = `sharing` + 1
  		WHERE documentId = #{documentId};
	</update>
	
	<update id="decreaseSharing" parameterType="document">
  		UPDATE document
  		SET
			`sharing` = `sharing` - 1
  		WHERE documentId = #{documentId};
	</update>
	
	<delete id="delete" parameterType="int">
		DELETE
		FROM document
		WHERE documentId = #{documentId};
	</delete>
</mapper>